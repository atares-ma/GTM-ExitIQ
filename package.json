{
  "name": "gtm-exitiq",
  "private": true,
  "description": "GTM ExitIQ - static site in site/, optional Vercel mail relay in api/",
  "dependencies": {
    "nodemailer": "^7.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "@types/nodemailer": "^6.4.0"
  }
}
